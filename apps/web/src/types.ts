/**
 * Domain types now live in @livedrop/core (zod schemas, shared across web,
 * server and the upcoming mobile app). Re-exported here so existing
 * `../types` imports keep working unchanged.
 */
export * from '@livedrop/core';
